json.extract! shop, :id, :stall_name, :stall_addres, :stall_pincode, :stall_city, :stall_state, :stall_details, :stall_link, :stall_registraton_status, :building_id, :created_at, :updated_at
json.url shop_url(shop, format: :json)
